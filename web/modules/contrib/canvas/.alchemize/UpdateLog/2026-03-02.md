# Update log — 2026-03-02

Documentation of the upstream sync performed on 2026-03-02 using the process in [agent-update.md](../agent-update.md). **Document only** — no further code or branch changes.

## Snapshot (previous working state)

Before the update, pre-sync tags were created so every branch could be restored if the sync failed:

- **Tag prefix:** `pre-sync/20260302/*`
- **Previous integration (restore point):** `pre-sync/20260302/integration` → commit `2c495705` ("plan md added")
- **Per-branch restore:** e.g. `pre-sync/20260302/feat/per-content-editing-frontend`, `pre-sync/20260302/fix/autosave-revision-ordering`, etc., for all branches listed in `sync-branches.conf`

To restore everything to this state after a failed sync:

```bash
DATE=20260302
for branch in $(grep -v '^\s*#' .alchemize/sync-branches.conf | grep -v '^\s*$'); do
  short="${branch#local/}"
  git checkout "$branch" && git reset --hard "pre-sync/${DATE}/${short}"
done
git checkout local/integration && git reset --hard "pre-sync/${DATE}/integration"
```

## What was done

1. **Reconnaissance** — Fetched upstream, compared `local/integration` to `upstream/1.x`, identified new upstream commits and overlap with our branches.
2. **Plan** — Decided which branches to keep, adapt, or drop; noted conventions to adopt (e.g. PHP backslash-prefixed built-ins, `#[Group(...)]` in tests).
3. **Rebase** — Each local branch in `.alchemize/sync-branches.conf` was rebased onto `upstream/1.x` (conflicts resolved inline where needed).
4. **Review & cleanup** — Each rebased branch was reviewed for upstream-readiness (conventions, no integration-only leakage, tests on `CanvasKernelTestBase` where applicable).
5. **Rebuild integration** — `local/integration` was reset to `upstream/1.x`, then all branches were merged in the order defined in `sync-branches.conf`. Expected merge conflicts between overlapping branches (e.g. expose-slot-dialog-ui vs per-content-editing-frontend) were resolved.
6. **Restore tooling** — `.alchemize/` was restored from the pre-sync state and committed ("Restore .alchemize/ tooling on integration branch").
7. **Build artifacts** — `npm install`, build of `packages/drupal-canvas`, build of `ui/`, then `.gitignore` adjusted and build artifacts committed ("Include build artifacts in integration branch").

## Upstream changes incorporated

- **Fork point (our previous base):** `9aefa521`
- **New upstream tip:** `06d7936a` (current `upstream/1.x` at time of sync)
- **Count:** 18 new upstream commits

Notable upstream changes in that range (from `git log 9aefa521..upstream/1.x`):

| Commit     | Summary |
|-----------|---------|
| 06d7936a  | chore: Convert `@group foo` to `#[Group(…)]` + PHPCS enforcement for test subsets |
| 1d5b2a52  | feat: UI Support for Multi-Value Prop Configuration for Code Components |
| 1c1c5a46  | chore: PHPCS — `is_string()` → `\is_string()`, `is_array()` → `\is_array()`, etc. |
| cd10ad6e  | fix: Follow-up 500 for code component preview after deleting required prop (unexposed components) |
| 86ed3b2b  | fix: SPA navigation improvements in Publishing UX (part 2) |
| 034c2c56  | fix: Exclude `target_uuid` from default content exports |
| 912a8c64  | feat: User has no way of knowing required prop needs example value (theme builder) |
| 3a25554b  | feat: Prototype Canvas Workbench |
| 39de79b8  | feat: Utils for converting between Canvas component trees and json-render |
| 1955771a  | fix(AI): Harden image download |
| 9ca5c95e  | feat: DX+CI — `composer run phpstan-regenerate-ignores-for-11.3-and-higher` |
| 0522ab21  | fix: Component preview for code components in Safari |
| 4e2c0878  | fix: 500 error for code component preview after deleting required prop |
| b6f95ff2  | chore: PHPStan — stop ignoring invalid @covers annotations |
| 707f0a4f  | fix: Description for checkboxes should go to next line (Redux field widgets) |
| 014d20d2  | fix: JsComponent — getScopedDependencies/getDependencyLibraries and auto-saves |
| f47cc062  | chore: Fix PHPStan errors in HEAD after 2.1.40 release |
| 96fc3d98  | feat: Resizable right sidebar for Drupal Canvas |

## Branches updated (merge order)

As in `.alchemize/sync-branches.conf`:

**Phase 1 — Fixes:**  
optional-field-mapping, computed-url-optional-field, viewbuilder-label-field, validator-scoped-parent, hydration-orphan-parent, access-check-graceful-403, autosave-revision-ordering, preview-empty-optional-props

**Phase 2 — Features:**  
active-exposed-slots, merged-component-tree, field-widget-component-tree, auto-provision-slot-fields, canvas-fields-api, generalize-content-api, entity-layout-tab, per-content-editing-backend, expose-slot-dialog-ui, per-content-editing-frontend

## Resulting integration

- **Branch:** `local/integration`
- **Top commits (first-parent):**  
  - Include build artifacts in integration branch  
  - Restore .alchemize/ tooling on integration branch  
  - Merge feat/per-content-editing-frontend  
  - (then merge commits for each branch in order, then upstream history)

All 18 local branches are merged into `local/integration`; working tree clean. No branches were dropped or added for this sync.

## Reference

- **Process:** [.alchemize/agent-update.md](../agent-update.md)
- **Branch list and merge order:** [.alchemize/sync-branches.conf](../sync-branches.conf)
- **Design and script:** [.alchemize/update-process.md](../update-process.md)
